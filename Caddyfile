# Replace ":80" with your real domain, e.g. "guesthouse.example.com",
# when you are ready to expose the service on the public internet.
:80 {
    encode zstd gzip
    reverse_proxy guesthouse:8000
}
